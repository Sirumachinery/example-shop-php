{% extends 'base.html.twig' %}

{% block body %}

    <hr/>

    <div class="row">
        <div class="col-md-10 col-md-offset-1">
            <div class="row">
                <div class="col-md-10 col-md-offset-1">
                    <div class="alert alert-danger text-center">
                        <h2>{{ "purchase.error" | trans }}</h2>
                        <h4 class="text-muted">{{ "purchase.hint" | trans }}</h4><br/>
                        <p><a href="{{ path('demoshop_index') }}" class="btn btn-default btn-lg">{{ "purchase.back" | trans }}</a></p>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
