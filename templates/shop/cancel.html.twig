{% extends 'base.html.twig' %}

{% block body %}

    <hr/>

    <div class="row">
        <div class="col-md-10 col-md-offset-1">
            <div class="row">
                <div class="col-md-10 col-md-offset-1">
                    <div class="alert alert-warning text-center">
                        <h2>{{ "purchase.canceled" | trans }}</h2>
                        <p><a href="{{ path('demoshop_index') }}" class="btn btn-default btn-lg">{{ "purchase.back" | trans }}</a></p>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
